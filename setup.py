# setup.py
from setuptools import setup, find_packages

setup(
    name="pixel_art_renderer",
    version="0.0.1",
    author="Charles Morman",
    packages=find_packages(),
    install_requires=[],
)
